import type React from "react";

const Header: React.FC = () => {
	return (
		<header className="bg-gray-800 text-white p-8 flex justify-between items-center">
			{/* <h1 className="text-3xl font-bold">Sebastian Martini</h1> */}
		</header>
	);
};

export default Header;
